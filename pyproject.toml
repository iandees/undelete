[project]
name = "osm-undelete"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "boto3",
    "python-dotenv",
    "geopandas",
    "pyarrow",
    "shapely",
]

[dependency-groups]
dev = [
    "pytest",
]
