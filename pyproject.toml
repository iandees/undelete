[project]
name = "osm-undelete"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "boto3",
    "python-dotenv",
    "geopandas",
    "pyarrow",
    "shapely",
    "pyyaml>=6.0.3",
    "lark",
]

[dependency-groups]
dev = [
    "pytest",
]
